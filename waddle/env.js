// set env to 'production' if you want to test against the deployed http server on Heroku
// or to 'development' if you want to test against a local instance of the http server (not
// the app-dev-server, which is provided by Xcode in project's AppDelegate.m file)
var env = 'development'; 
module.exports = env;
