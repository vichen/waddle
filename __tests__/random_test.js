'use strict';

describe('random test', function() {
  it('returns true always', function() {
    expect(true).toBe(true);
  });
});

// Require depedencies needed for testing

// Tests for basic server functionality
  // Server config file should exist
  // Should accept and respond to GET requests at /match endpoint
  // Should accept and respond to GET requests at /authenticate endpoint

// Tests for basic database functionality
  // Should add new records to the database
  // Should remove records from the database
  // Should contain records that have not been added