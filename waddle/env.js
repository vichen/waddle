// set env to 'production' if you want to test against the deployed server on Heroku
// or to 'development' if you want to test against a locally hosted server (either
// localhost or a LAN ip address)
var env = 'development'; 
module.exports = env;
